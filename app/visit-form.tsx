import React, { useState } from 'react';
import { Alert, Image, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import * as ImagePicker from 'expo-image-picker';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { Feather } from '@expo/vector-icons';
import { router } from 'expo-router';
import { useColors } from '@/hooks/useColors';
import { useStore, visitTypes } from '@/context/AppContext';
export default function VisitForm() { const c = useColors(); const insets = useSafeAreaInsets(); const { schools, addVisit } = useStore(); const [schoolId, setSchoolId] = useState(schools[0]?.id || ''); const [type, setType] = useState(visitTypes[0]); const [date, setDate] = useState('2026-08-20'); const [actions, setActions] = useState(''); const [proposals, setProposals] = useState(''); const [photoUri, setPhotoUri] = useState<string | undefined>(); const takePhoto = async () => { const permission = await ImagePicker.requestCameraPermissionsAsync(); if (!permission.granted) return Alert.alert('صلاحية الكاميرا', 'اسمح للتطبيق باستخدام الكاميرا لتصوير سجل الزيارة'); const result = await ImagePicker.launchCameraAsync({ mediaTypes: ['images'], quality: .8, allowsEditing: true }); if (!result.canceled) setPhotoUri(result.assets[0]?.uri); }; const save = () => { if (!schoolId) return Alert.alert('بيانات ناقصة', 'اختر المدرسة ونوع الزيارة'); addVisit({ schoolId, date, type, actions, proposals, status: 'completed', photoUri }); router.back(); }; return <View style={[styles.page, { backgroundColor: c.background }]}><View style={[styles.header, { paddingTop: insets.top + 12 }]}><Pressable onPress={() => router.back()}><Feather name="arrow-right" size={23} color={c.foreground} /></Pressable><Text style={[styles.title, { color: c.foreground }]}>تسجيل زيارة</Text><View style={{ width: 24 }} /></View><ScrollView contentContainerStyle={{ paddingBottom: 45 }} keyboardShouldPersistTaps="handled"><Text style={[styles.label, { color: c.foreground }]}>المدرسة</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chips}>{schools.map(s => <Pressable key={s.id} onPress={() => setSchoolId(s.id)} style={[styles.chip, { backgroundColor: schoolId === s.id ? c.primary : c.card, borderColor: schoolId === s.id ? c.primary : c.border }]}><Text style={{ color: schoolId === s.id ? c.primaryForeground : c.foreground, fontFamily: 'Inter_500Medium', fontSize: 11 }}>{s.name}</Text></Pressable>)}</ScrollView><Text style={[styles.label, { color: c.foreground }]}>نوع الزيارة</Text><View style={styles.wrap}>{visitTypes.map(t => <Pressable key={t} onPress={() => setType(t)} style={[styles.type, { backgroundColor: type === t ? c.secondary : c.card, borderColor: type === t ? c.primary : c.border }]}><Text style={{ color: type === t ? c.primary : c.foreground, fontFamily: 'Inter_500Medium', fontSize: 11 }}>{t}</Text></Pressable>)}</View><Field label="تاريخ الزيارة" value={date} onChangeText={setDate} c={c} /><Field label="الإجراءات المتخذة" value={actions} onChangeText={setActions} placeholder="ما تم اتخاذه فعليًا أثناء الزيارة" c={c} multiline /><Field label="المقترحات والتوصيات" value={proposals} onChangeText={setProposals} placeholder="ما يقترحه المشرف للمتابعة والمعالجة" c={c} multiline /><Pressable onPress={takePhoto} style={[styles.camera, { backgroundColor: c.secondary, borderColor: c.border }]}><View style={[styles.cameraIcon, { backgroundColor: c.primary }]}><Feather name="camera" size={20} color={c.primaryForeground} /></View><View style={{ flex: 1 }}><Text style={[styles.cameraTitle, { color: c.foreground }]}>{photoUri ? 'تم إرفاق صورة السجل' : 'تصوير سجل الزيارة'}</Text><Text style={[styles.cameraSub, { color: c.mutedForeground }]}>التقط صورة مباشرة من كاميرا الهاتف</Text></View><Feather name="chevron-left" size={18} color={c.mutedForeground} /></Pressable>{photoUri && <Image source={{ uri: photoUri }} style={styles.preview} />}<Pressable onPress={save} style={[styles.save, { backgroundColor: c.primary }]}><Feather name="check" size={19} color={c.primaryForeground} /><Text style={{ color: c.primaryForeground, fontFamily: 'Inter_700Bold' }}>حفظ الزيارة</Text></Pressable></ScrollView></View>; }
function Field({ label, value, onChangeText, placeholder, multiline, c }: any) { return <View style={styles.field}><Text style={[styles.label, { color: c.foreground }]}>{label}</Text><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor={c.mutedForeground} multiline={multiline} style={[styles.input, { backgroundColor: c.card, borderColor: c.border, color: c.foreground, minHeight: multiline ? 80 : 48 }]} textAlign="right" textAlignVertical={multiline ? 'top' : 'center'} /></View>; }
const styles = StyleSheet.create({ page: { flex: 1, paddingHorizontal: 18 }, header: { flexDirection: 'row-reverse', alignItems: 'center', justifyContent: 'space-between', paddingBottom: 16 }, title: { fontFamily: 'Inter_700Bold', fontSize: 20 }, label: { fontFamily: 'Inter_600SemiBold', fontSize: 12, textAlign: 'right', marginTop: 10, marginBottom: 8 }, chips: { flexDirection: 'row', gap: 8, paddingBottom: 3 }, chip: { borderWidth: 1, borderRadius: 12, paddingHorizontal: 12, paddingVertical: 10 }, wrap: { flexDirection: 'row-reverse', flexWrap: 'wrap', gap: 7 }, type: { borderWidth: 1, borderRadius: 11, paddingHorizontal: 10, paddingVertical: 9 }, field: { marginTop: 5 }, input: { borderWidth: 1, borderRadius: 13, paddingHorizontal: 13, paddingVertical: 11, fontFamily: 'Inter_400Regular', fontSize: 13, marginBottom: 2 }, camera: { borderRadius: 16, borderWidth: 1, padding: 12, flexDirection: 'row-reverse', alignItems: 'center', gap: 10, marginTop: 15 }, cameraIcon: { width: 42, height: 42, borderRadius: 13, alignItems: 'center', justifyContent: 'center' }, cameraTitle: { fontFamily: 'Inter_600SemiBold', textAlign: 'right', fontSize: 13 }, cameraSub: { fontFamily: 'Inter_400Regular', textAlign: 'right', fontSize: 10, marginTop: 4 }, preview: { width: '100%', height: 170, borderRadius: 15, marginTop: 10 }, save: { minHeight: 52, borderRadius: 15, flexDirection: 'row-reverse', alignItems: 'center', justifyContent: 'center', gap: 8, marginTop: 16 } });
